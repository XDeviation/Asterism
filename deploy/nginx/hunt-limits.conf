limit_req_zone $binary_remote_addr zone=hunt_login:10m rate=10r/m;
limit_conn_zone $binary_remote_addr zone=hunt_connections:10m;

map $http_upgrade $hunt_connection_upgrade {
    default upgrade;
    '' close;
}
